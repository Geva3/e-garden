<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.e_garden">

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Egarden">
        <activity android:name=".viewModels.Home" />
        <activity android:name=".viewModels.AddPlant"
            android:parentActivityName=".viewModels.Home"/>
        <activity android:name=".viewModels.TesteListView"
            android:parentActivityName=".viewModels.Home"/>
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewModels.ListViewModel" />
    </application>

</manifest>